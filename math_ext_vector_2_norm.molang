// fn.math_ext_vector_2_norm(vector1) => vector_2 二维向量归一化

t.v1 = args[0];

// 归一化
t.length = fn.math_ext_vector_2_len(t.v1);

t.result = (t.length > 0.000000000001) ? 
    fn.math_ext_vector_2(
        t.v1.x / t.length,
        t.v1.y / t.length
    ) :
    t.v1;

return t.result;
