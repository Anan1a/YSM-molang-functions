// fn.mc_get_relPos(name) => vector_3 获取相对坐标（组名）

t.name = args[0];

t.point = fn.math_ext_vector_3(
    ysm.bone_pos(t.name).x,
    ysm.bone_pos(t.name).y,
    ysm.bone_pos(t.name).z
);

return t.point;
