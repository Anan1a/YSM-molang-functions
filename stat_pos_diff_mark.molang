// fn.stat_pos_diff_mark(pos_diff) 记录位置（坐标组）

t.result = args[0];

// 还原结构体(仅更改)
t.wpos = fn.mc_get_worldPos;
t.result.mark_world_pos_x = t.wpos.x;
t.result.mark_world_pos_y = t.wpos.y;
t.result.mark_world_pos_z = t.wpos.z;

return t.result;
