// fn.mc_get_prevRot(name) => vector_3 获取上一帧旋转（组名）

t.name = args[0];

t.point = fn.math_ext_vector_3(
    ysm.bone_rot(t.name).x,
    ysm.bone_rot(t.name).y,
    ysm.bone_rot(t.name).z
);

return t.point;
